<div class="row">
  <nav class="top-bar colum" data-topbar>

    <ul class="title-are">
      <li class="nam">
        <h1>
          <a href="/movies">
            <i class="fi-play-video"></i> MovieStore
          </a>
        </h1>
      </li>
      <li class="toggle-topbar"><a href="#"><span>Menu</span></a></li>
    </ul>

    <section class="top-bar-sectio">

      <!-- Right Nav Section -->
      <ul class="right">
        <%if current_muser %>
            <li>
              <%= link_to cart_path do%>
                  <i class="fi-shopping-cart"></i> My Cart (<span class="cart-count"><%=current_muser.cart_count%></span>)
              <%end%>
            </li>
            <li><%=link_to current_user.email, edit_muser_registration_path, class: "blackcolorbold"%></li>
            <li><%=link_to "Logout", destroy_muser_session_path, method: :delete, class: "blackcolorbold"%></li>
        <%else%>
            <li><%=link_to "Register", new_muser_registration_path, class: "blackcolorbold" %></li>
            <li><%=link_to "Login", new_muser_session_path, class: "blackcolorbold"%></li>
        <%end%>
      </ul>

    </section>

  </nav>
</div>
